// vite.config.js
export default {
    root: '.', // Ensure this points to where your index.html is located
    build: {
      outDir: 'dist', // Where the production build will be output
    },
  };
  